##### Copy this snapshot in the overview panel

```js
import React from "react";

export default <div>You should render the ToastContainer in the bootstrapper of the application(like index.js)
and use useToast hook for toasting a message. See the examples from <a href="https://esignapp.appservice.online/flows">this</a>.
	See the base code of Document Signer as for further understanding.</div>
```
