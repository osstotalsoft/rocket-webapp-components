##### Copy this snapshot in the overview panel
 ```js
import React from 'react';
import CustomDialog from '@bit/totalsoft_oss.react-mui.custom-dialog';

export default (
		<CustomDialog
			id={"id"}
			open={true}
			title={"To see the entire functionality of the component drag the paint window to right"}
			content={<div>To see the entire functionality of the component drag the paint window to right</div>}
			textContent={"To see the entirely functionality of the component drag the paint window to right "}
			fullWidth
			maxWidth
			showActions
			buttonColor="theme"
		/>
)
 ```


 ##### Copy this snapshot in the overview panel
 ```js
import React from 'react';
import CustomDialog from '@bit/totalsoft_oss.react-mui.custom-dialog';

export default (
	<CustomDialog
		id={"id"}
		open={true}
		title={"To see the entire functionality of the component drag the paint window to right"}
		content={<div>To see the entire functionality of the component drag the paint window to right</div>}
		textContent={"To see the entirely functionality of the component drag the paint window to right "}
		maxWidth="xs"
		showActions
		buttonColor="info"
	/>
)
 ```


 
 ##### Copy this snapshot in the overview panel
 ```js
import React from 'react';
import CustomDialog from '@bit/totalsoft_oss.react-mui.custom-dialog';

export default (
	<CustomDialog
		id={"id"}
		open={true}
		title={"To see the entire functionality of the component drag the paint window to right"}
		content={<div>To see the entire functionality of the component drag the paint window to right</div>}
		textContent={"To see the entirely functionality of the component drag the paint window to right "}
		maxWidth="lg"
		showActions
		buttonColor="info"
	/>
)
 ```